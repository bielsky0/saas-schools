### Faza 5 — Publiczny zapis + płatność na miejscu + współbieżność end-to-end

**Status:** ✅ **zakończona** (2026-07-21)
**Cel:** pierwszy pełny przepływ wartości: rodzic zapisuje dziecko przez publiczny link, płaci na miejscu, wszystkie trzy ochrony §5 działają na żywej ścieżce.
**Pokrywa:** EPIK 4 (US-4.1–4.3, US-4.4, US-4.5, US-4.6 — **bez US-4.2/AC4–AC5 v15**, patrz D78), EPIK 6 (US-6.2), EPIK 14 (US-14.1–14.3), EPIK 15; §2.3, §2.7 (Proces A — fundament).
**Zależności:** F2 (typy/sesje), F3 (client+OTP), F4 (kredyty — ścieżka `booked_offline` nie konsumuje ich jeszcze, ale model jest wspólny), F4.5/F4.6 (routing + host tenanta).
**Migracja bazy: ŻADNA** — wszystkie tabele/kolumny/constrainty (booking, class_session, group_type, client, athlete, oba EXCLUDE) istnieją od F0; słownictwo audytu to unie TS nad `text` (bez `pgEnum`). `git status` na `migrations/` pusty; pułapka monotoniczności `when` (D46/D51/D67) nie dotyczy tej fazy.

**Decyzje produktowe podjęte na starcie (przed kodem):**

| #   | Pytanie                                                        | Decyzja                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A   | Płatność online, skoro F11/Stripe nie istnieje                | **Widoczna, zablokowana z komunikatem** — wzorzec §2.25/Constraint 7. `onlineAvailable` to jeden parametr (literał `false` w F5); F10/F11 podmienia warunek na `stripe_connect_charges_enabled`, nie UI    |
| B   | Kiedy powstaje `booking` względem OTP                          | **Dopiero po weryfikacji** (D74). Niezweryfikowany nie zajmuje pojemności cudzym e-mailem; §US-6.2 zabrania auto-zwalniania, więc zajęte miejsce byłoby zajęte na zawsze                                    |
| C   | Wybór terminu                                                 | **Kalendarz date-first, session-based.** Data → sloty tego dnia → sesja; zapytanie po `class_session.start_time` renderowane w `organization.timezone`                                                     |
| D   | Rozpoznanie klienta (US-4.2/AC1)                              | **Po cookie sesji, nie po wpisanym e-mailu** (D75) — dosłowne AC to oracle enumeracyjny                                                                                                                    |
| E   | Gdzie powstaje `athlete`                                      | **W transakcji bookingu**, po OTP (D76)                                                                                                                                                                    |
| F   | Oferta `payment_policy="online"` przy wyłączonym online       | **Komunikat + brak zapisu, PLUS egzekwowanie backendowe** metody płatności w `create.ts` (D77)                                                                                                             |

**Zrealizowany zakres:** `src/lib/datetime.ts` (wypromowany `zonedWallClockToUtc` z `recurrence.ts` + nowe `zonedPartsOf`/`zonedDayKey`/`zonedWallClock`/`monthRangeInZone`/`shiftMonth`/`wallClockToInstant`, 3 konsumentów przekierowanych: `recurrence.ts`, `schedule/actions.ts`, `credits/validity.ts`); `constraintOf`+`CONSTRAINT_BOOKING_ATHLETE_OVERLAP` w `sql-error.ts` (nazwa pola `constraint_name` **zweryfikowana empirycznie** wobec realnego błędu postgres-js, nie założona); wpis `/zapisy` w `public-routes.ts` (`indexable:false`) + przepisane dwa fałszywe komentarze w `reserved-slugs.ts`/`proxy.ts`; `bookings/data.ts` (`countActiveBookingsForSession`, `listSessionAvailability`); `bookings/calendar.ts` + `payment-options.ts` (czyste, Vitest); `bookings/create.ts` — jedyny pisarz §5.2 (`FOR UPDATE` → walidacja metody → własność dziecka → count vs capacity → insert z czasami z zablokowanego wiersza → `recordAudit`); `bookings/schema.ts` (participant jako unia dyskryminowana) + `bookings/actions.ts` (`resolveClientSession` + jawna asercja `isVerified`, nigdy `redirect()`); `clients/data.ts` (`insertAthlete`, `getOwnedAthlete`); `audit.ts` (`booking.create`, target `booking`, `ActorType` `Client`, `clientActor` z `actorId:null` wymuszonym FK); namespace i18n `enrollment` (PL first) + klucze audytu; `(site)/layout.tsx` + `(site)/zapisy/[groupTypeSlug]/page.tsx` (`requireServedOrganization()` jako pierwsza instrukcja, `generateMetadata` noindex bez apeksowego canonical) + `bookings/components/enrollment-flow.tsx`; poszerzony `/api/dev/seed-langlion` (paymentPolicy/allowedPurchaseModes/isNewClientOnly/isVerified/setGroupTypePrice) + nowy `/api/dev/bookings`; `uniqueNearFutureSlot` w `e2e/helpers.ts`; `e2e/enrollment-fixtures.ts` + `e2e/langlion-enrollment.spec.ts` (9 testów) + 2 testy w `langlion-subdomain-routing.spec.ts`.

#### Raport z realizacji Fazy 5 — referencja względem DoD

| Kryterium DoD                                            | Wynik                                                                                                                                                    |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Happy path z OTP → `booked_offline`                     | ✅ pełna ścieżka UI (data→slot→e-mail→OTP z outboxu→uczestnik→na miejscu); asercja **wiersza**: `booked_offline`, `price_snapshot={10000,PLN}`, czas sesji |
| Dwóch klientów na ostatnie miejsce — jeden sukces       | ✅ `Promise.all` na produkcyjny `createBooking`, `holdMs:400`; multizbiór `["created","session_full"]`, dokładnie 1 aktywna. **Zweryfikowane mutantem** (usunięcie `.for("update")` wywala test) |
| Kolizja zawodnika odrzucona niezależnie od roli         | ✅ nakładające sesje/typy/trenerzy → `23P01`/`booking_athlete_no_overlap_excl`; samonakładanie na tej samej sesji też (dowód: brak potrzeby unique index) |
| Cena zamrożona mimo zmiany na Definicji                 | ✅ `price_snapshot.amount=10000` po podniesieniu `group_type.price→12000`, a strona pokazuje 12000 (odróżnia „zamrożone" od „update się nie wykonał")     |
| `payment_pending` zajmuje miejsce                       | ✅ spec 8 przez seeder/dev-route — jedyna droga, bo przy wyłączonym online UI nie ma osiągalnej ścieżki do `payment_pending`                             |
| Macierz płatności (US-4.4)                              | ✅ on_site (brak online), packages-only (komunikat, brak kalendarza), online-only (none_available); **backend odrzuca metodę spoza polityki** (D77)      |
| Short path rozpoznanego klienta (US-4.2 wg D75)         | ✅ drugi zapis w tym samym browserze bez OTP, istniejące dziecko z `<select>`                                                                            |
| `is_new_client_only` nie jest bramką (US-4.3)           | ✅ jedyny dowód e2e — rozpoznany klient zapisuje ofertę z flagą                                                                                          |
| Routing `/zapisy`                                       | ✅ 404 na apeksie/nieznanej akademii, renderuje się na tenancie (nie 307 na `/login`). **Zweryfikowane mutantem** (usunięcie wpisu z PUBLIC_PAGE_ROUTES) |
| Migracje od zera                                        | ✅ faza nie dodała migracji; `git status` na `migrations/` pusty; `db:migrate` idempotentne                                                             |
| **Cała suita e2e**                                      | ✅ **212 passed, 3 skipped, 0 failed** (`--workers=1`, konfiguracja CI). Baseline F4.6: 201+3; +11 nowych (9 enrollment + 2 routing)                     |
| `lint` / `typecheck` / `test` / `format:check`          | ✅ wszystkie zielone (Vitest **99/99**, było 70; +29: datetime, calendar, payment-options)                                                              |

#### Rozstrzygnięcia podjęte w Fazie 5

| #   | Decyzja                                                                                          | Uzasadnienie                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D74 | **OTP → booking** (odwrócenie US-4.5/AC1, które mówi booking → OTP)                              | Niezweryfikowany obcy konsumowałby pojemność cudzym adresem, a §US-6.2 zabrania auto-zwolnienia miejsca — trzymane byłoby na zawsze. AC odwrócone świadomie, nie po cichu. Punkt egzekwowania: jawna asercja `principal.isVerified` w `bookings/actions.ts`                                                          |
| D75 | **Rozpoznanie klienta po cookie, nie po wpisanym e-mailu** (zmiana US-4.2/AC1)                   | Dosłowne AC („wpisany e-mail → prefill + pominięty OTP") jest oracle'em enumeracyjnym — ujawnia przynależność rodzica do akademii, dokładnie własność, za którą F3 zapłaciło jednolitymi odpowiedziami `issueOtp`. Realizacja: żywa sesja (cookie 30 dni) → short path; brak cookie → jednokrokowy OTP. Intencja AC spełniona, oracle zamknięty |
| D76 | **`athlete` powstaje w transakcji bookingu**, nie przy wysłaniu OTP                              | F3 upsertuje wyłącznie `client`, nie athlete (brief zakładał błędnie). Zapis przed OTP pozwalałby anonimowi wypalić kwotę planu `max_students` (§2.20) cudzym e-mailem i zostawiał sieroce rekordy. `insertAthlete` w transakcji — rollback zabiera dziecko, więc brak sierot i brak potrzeby klucza upsert          |
| D77 | **Egzekwowanie metody płatności na backendzie** (rozszerzenie Constraint 7, nowy AC pod US-4.4) | US-4.4/AC3 pokrywa tylko prezentację; nie chroni przed bezpośrednim żądaniem API ani race'em, w którym Connect traci `active` w trakcie wypełniania formularza. `isMethodAcceptable` w `create.ts` (nie w akcji) — F6/F11 dziedziczą bez powtarzania. W F5 warunek dostępności online to literał `false`                |
| D78 | **US-4.2/AC4–AC5 (rabat na formularzu, v15) przeniesione do F21**                                | Tabela `client_price_override` nie istnieje w schemacie. F5 pokrywa AC1–AC3 i AC6 (nowy/niezweryfikowany zawsze widzi cenę katalogową — trywialnie, bo brak logiki override). W `create.ts` przy `priceSnapshot` należy w F21 podmienić `groupType.price` na `resolvePrice(client, groupType)`                        |
| D79 | **`/zapisy` `indexable:false`** (mechaniczne, nie preferencja SEO)                               | Oba konsumenty `indexable` (`sitemap.ts`/`robots.ts`) budują URL przez `absoluteUrl()` = apeks, gdzie `/zapisy/*` daje 404. Indeksowanie wstawiłoby URL gwarantowanie-404 do sitemapy per locale. Strona sama ustawia `robots:{index:false}` w `generateMetadata`, bez apeksowego canonical z `pageMetadata`         |
| D80 | **Bezpieczeństwo apeksowego `forward()` przeniesione z „braku trasy" na `requireServedOrganization()`** | Wczesny `return forward()` na apeksie dla prefiksów `tenant` omija default-deny; było nieszkodliwe **tylko** dlatego, że `/zapisy` nie miało trasy. F5 dodała trasę → obroną jest `requireServedOrganization()` jako pierwsza instrukcja strony (notFound na apeksie/obcym/nieznanym). Komentarze w `reserved-slugs.ts`/`proxy.ts` przepisane, zweryfikowane mutantem w spec routingu |

---

