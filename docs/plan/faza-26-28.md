## Fazy 26–28 — dalsze poprawki konkurencyjne (spec v18)

Trzy fazy realizujące trzy poprawki (ActiveNow) + przegląd zakresu odłożonego. Każda niesie **jawny blast radius** (dwie listy: zakończone-do-dotknięcia / nierozpoczęte-rosnące). Nie tworzą wspólnego bloku fazowego — wpinają się po swoich zależnościach.

---

### Faza 26 — Moduł obozów: karta kwalifikacyjna uczestnika wypoczynku (poprawka #7)

**Status:** ✅ zakończona (2026-07-26)
**Cel:** akademia prowadząca kolonie/półkolonie zbiera w systemie prawnie wymaganą kartę kwalifikacyjną uczestnika wypoczynku (dwie fazy: rodzic przed / kierownik po).
**Pokrywa:** EPIK 41; §2.40; §2.10 (`qualification_cards.manage`, `qualification_card.complete_return`); Constraint 16; Rozstrzygnięcie spec #32.
**Zależności:** F5 ✅ (formularz zapisu, `insertAthlete`, `enrollment-flow.tsx`), F24 ✅ (bramka `athlete_health.view` + encje `consent_document`/`athlete_consent` — reużyte), F2 ✅ (CRUD `group_type` — kolumna `requires_qualification_card`), storage §21 (eksport PDF — odłożony do osobnego PR). **Miękka:** F17 ✅ (regulaminy).

**Rozstrzygnięte otwarte punkty (§8):**
- #19 → twarda blokada: karta blokuje finalizację zapisu (`QualificationCardRequiredError` w `createBooking`)
- #20 → PDF przez storage §21 (TODO: implementacja w osobnym PR)
- #21 → override overlay (F23): `qualification_card.complete_return` w mapie tylko Owner/Admin, kierownik przez F23 grant

**Zrealizowano:**
- `0051_faza26_qualification_card.sql` — tabela `qualification_card` + kolumna `group_type.requires_qualification_card` (boolean, default false)
- `0052_rls_qualification_card.sql` — RLS tenant_isolation + system_bypass
- `src/lib/db/schema/qualification-cards.ts` — Drizzle schema: `qualificationCard` z FK kompozytowymi (athlete, group_type, file, user), Constraint 16 unique `(group_type_id, athlete_id)`
- `src/lib/db/schema/group-types.ts` — kolumna `requiresQualificationCard`
- `src/features/rbac/index.ts` — uprawnienia `qualification_cards.manage` (Owner, Admin, Secretariat) i `qualification_card.complete_return` (Owner, Admin + override F23)
- `src/features/qualification-cards/schema.ts` — zod: `createParentPhaseSchema`, `createLeaderPhaseSchema`, `qualificationCardStatus` enum
- `src/features/qualification-cards/data.ts` — CRUD: `getQualificationCard`, `getQualificationCardById`, `listQualificationCards`, `upsertQualificationCard`, `completeLeaderPhase`, `setQualificationCardFileId`
- `src/features/qualification-cards/actions.ts` — server actions: `completeParentPhaseAction` (parent, client auth), `completeLeaderPhaseAction` (staff, `qualification_card.complete_return`)
- `src/features/qualification-cards/components/qualification-card-form.tsx` — reusable form (6 pól textarea), używany w enrollment-flow i standalone page
- `src/features/bookings/create.ts` — `QualificationCardRequiredError` + walidacja karty po consentach, przed capacity (krok 4.6)
- `src/features/bookings/create-many.ts` — propagacja `requiresQualificationCard` + error label `qualificationCardRequired`
- `src/features/bookings/actions.ts` — `requiresQualificationCard` w `createBookingAction` + `createBookingManyAction` + error handling
- `src/features/bookings/components/enrollment-flow.tsx` — prop `requiresQualificationCard` + notice w `ConfirmStep` (amber box z linkiem do `/karta/[groupTypeSlug]`)
- `src/features/groups/schema.ts` — `requiresQualificationCard: z.boolean().default(false)`
- `src/features/groups/actions.ts` — parse + insert/update + `changed()` dla nowej kolumny
- `src/features/groups/components/group-type-form.tsx` — checkbox `requiresQualificationCard`
- `src/app/[locale]/(site)/karta/[groupTypeSlug]/page.tsx` — standalone strona: lista dzieci + formularze kart z istniejącymi danymi
- `src/app/[locale]/(app)/dashboard/qualification-cards/page.tsx` — admin panel: lista kart z filtrem statusu
- `src/features/admin/audit.ts` — akcje `qualification_card.parent_completed`, `qualification_card.leader_completed` + target `qualification_card`
- i18n `en.json`/`pl.json` — klucze `qualificationCards.*`, `enrollment.qualificationCard.*`, `groups.form.requiresQualificationCard`
**DoD:** e2e: admin oznacza ofertę jako obozową; rodzic wypełnia część przed (karta `parent_completed`, jedna per uczestnik per obóz — Constraint 16); dane zdrowotne niewidoczne bez `athlete_health.view`, odrzucone na backendzie (wzorzec US-37.3/US-41.3); kierownik z `qualification_card.complete_return` domyka część po (`leader_completed`), a użytkownik bez tego uprawnienia dostaje odmowę; eksport karty; suita zielona.

**⚠️ Blast radius (poprawka #7):**
- **Zakończone fazy do ponownego dotknięcia:** **F2 ✅** (`group_type` CRUD + zod — kolumna `requires_qualification_card`); **F24** (jeśli zamknięta przed F26) — karta **reużywa** `athlete_health.view` i encje zgód **bez zmiany ich sygnatur** (spójność wymuszona modelem, nie retrofit); **F5 ✅** dotknięta **tylko jeśli** §8 #19 = „karta blokuje finalizację zapisu" (dodatkowy krok w `enrollment-flow.tsx`) — inaczej nietknięta.
- **Nierozpoczęte fazy rosnące bez ryzyka retrofitu:** **F13** (panel klienta — wypełnianie/edycja części rodzica, podgląd statusu karty); **F14** (zdarzenie `qualification_card_requested`, jeśli #24/#19 je wprowadza).

---

### Faza 27 — Opłaty dodatkowe ad-hoc (extra_fee) (poprawka #8)

**Status:** ✅ zakończona (2026-07-26)
**Cel:** akademia nakłada jednorazowe opłaty niezwiązane z sesją/pakietem (strój, materiały, wpisowe, wycieczka), pojedynczo i zbiorczo, bez naciągania modelu kredytowego.
**Pokrywa:** EPIK 42; §2.41; §2.10 (`extra_fees.manage`); Constraint 17; Zasada (v18) „extra_fee poza systemem kredytowym" (§1.3); Rozstrzygnięcia spec #33–#36.
**Zależności:** **F10** (Stripe Connect — online `extra_fee` na Connected Account, Zasada #7), **F11** (ścieżka pojedynczej płatności online / ad-hoc `price_data`), F3 ✅ (`client`). Fakturowanie wpina się w F19 (§2.17, lista fakturowania).
**Migracja:** additive (`extra_fee`) + RLS wg wzorca `0015`; grep kolizji eksportów (D11); monotoniczność `when` (D46/D51/D67); **brak** pól refund (Rozstrzygnięcie #33); Constraint 17 egzekwowany w orkiestratorze zbiorczym (app-level, jak Constraint 15).
**Zakres:** tabela `extra_fee` + RLS; uprawnienie `extra_fees.manage` w statycznej mapie RBAC; tworzenie **pojedyncze** z profilu klienta/uczestnika; **orkiestrator zbiorczy** (osobny plik, per uczestnik osobna transakcja, raport zbiorczy — wzorzec §2.11/Mass Move Bookings, ale bez capacity/kolizji); online przez adapter billingowy → **Connected Account** (ad-hoc `price_data`, Zasada #7; bramka Constraint 7 — online wymaga `stripe_connect_status=active`); cash z potwierdzeniem personelu; anulowanie/soft delete jako jedyna korekta (**brak** Stripe Refund — #33); pola fakturowania jak `credit_purchase` (#36); `recordAudit`. **Extra_fee nigdy nie dotyka `credit`** (Zasada v18) — nie pojawia się w portfelu (§7.12).
**DoD:** e2e: recepcja tworzy extra_fee pojedynczo (bez generowania kredytu, nie w portfelu); nałożenie zbiorcze na sesję → raport zbiorczy (Constraint 17); płatność online na Connected Account, cash z potwierdzeniem; anulowanie = `cancelled`/soft delete, **brak** ścieżki zwrotu; extra_fee bez `extra_fees.manage` odrzucone na backendzie; suita zielona.

**⚠️ Blast radius (poprawka #8):**
- **Zakończone fazy do ponownego dotknięcia:** **ŻADNA** — `extra_fee` to nowa, izolowana encja; reużywa adapter billingowy (F10/F11) **bez zmiany ich sygnatur**. Warunek: online `extra_fee` wymaga gotowej ścieżki ad-hoc `price_data` z F11/F12 — stąd zależność, nie retrofit.
- **Nierozpoczęte fazy rosnące bez ryzyka retrofitu:** **F13** (panel klienta — widok extra_fee, jeśli §8 #22 = tak); **F19** (lista ręcznego fakturowania rozszerzona o `extra_fee` — §2.17).

---

### Faza 28 — Tematy lekcji i śledzenie prac domowych (poprawka #9)

**Status:** ✅ zakończona (2026-07-26)
**Cel:** personel zapisuje strukturalny temat lekcji i zadaje pracę domową z odznaczaniem wykonania per uczestnik — osobno od ocen/notatek e-dziennika.
**Pokrywa:** EPIK 43; §2.42; §2.10 (`lesson_log.manage`); Constraint 18; Rozstrzygnięcie spec #31.
**Zależności:** **Faza 6** (e-dziennik **EPIK 35** — lista uczestników sesji §16.1, wzorzec RBAC `grade_fields.manage`/`grades.enter`, wzorzec „osobna oś" §2.29/Constraint 12). **Jawnie:** funkcjonalność dzieli infrastrukturę z EPIK 35/Faza 6 — **nie buduje równoległego mechanizmu**. **Miękkie:** F13 (widok klienta — temat/zadanie/status), F14 (katalog powiadomień — seed `homework_assigned`/`lesson_topic_added`).
**Migracja:** additive (`lesson_topic`, `homework`, `homework_completion`) + RLS wg wzorca `0015`; grep kolizji eksportów (D11 — zwłaszcza ogólne nazwy `homework`); monotoniczność `when` (D46/D51/D67); unique `(homework_id, athlete_id)` (Constraint 18); tabele nie modyfikują `booking` → nie dotykają EXCLUDE §5.3.
**Zakres:** rozszerzenie strony personelu (lista uczestników sesji z F6) o sekcje „Szczegóły lekcji" (`lesson_topic`) i „Praca domowa" (`homework`); `homework_completion` oznaczane przez **personel** (Rozstrzygnięcie #31), kolumna rezerwowa `completed_by_actor_type` (zawsze `staff`) pod przyszłe oznaczanie przez rodzica; uprawnienie `lesson_log.manage` (Owner/Admin wszystkie, Trener tylko własne, backend-enforced — jak `grades.enter`); niezależność osi (Constraint 18 — status wykonania nie rusza obecności/płatności); powiadomienie klienta e-mailem od razu (e-mail-first; retrofit in-app w F14 — dopisanie `homework_assigned`/`lesson_topic_added` do seeda katalogu); widok klienta = retrofit F13.
**DoD:** e2e: trener wpisuje temat i zadaje pracę domową z listy uczestników własnej sesji; na cudzej sesji dostaje odmowę z backendu (US-43.1/AC3); oznacza `homework_completion` per uczestnik (osobna oś — nie zmienia `attendance_status`/`payment_status`, Constraint 18); klient dostaje e-mail o nowym wpisie; `lesson_log.manage` egzekwowane na backendzie; suita zielona.

**⚠️ Blast radius (poprawka #9):**
- **Zakończone fazy do ponownego dotknięcia:** **ŻADNA** — nowe tabele, osobna oś, independent axis. Strona uczestników z F6 rozszerzona o dwie sekcje bez zmiany istniejących kontraktów.
- **Nierozpoczęte fazy rosnące bez ryzyka retrofitu:** **F13** (panel klienta — temat/zadanie/status), **F14** (seed powiadomień).

**Zrealizowano:**
- `0055_faza28_lesson_log.sql` — 3 tabele: `lesson_topic` (unique `(session_id, organization_id)`), `homework` (session_id NOT NULL), `homework_completion` (unique `(homework_id, athlete_id)`, Constraint 18) z kolumną rezerwową `completed_by_actor_type` (default `staff`)
- `0056_rls_lesson_log.sql` — RLS tenant_isolation + system_bypass (wzorzec `0054`)
- `0057_seed_lesson_notifications.sql` — seed `notification_event_type` dla `lesson_topic_added` i `homework_assigned` (oba `is_overridable=tak`)
- `src/lib/db/schema/lesson-logs.ts` — Drizzle schema: `lessonTopic`, `homework`, `homeworkCompletion` z kompozytowymi FK + ON DELETE decyzjami (session_id → RESTRICT, homework_id → CASCADE, created_by/marked_by → RESTRICT)
- `src/features/rbac/index.ts` — uprawnienie `lesson_log.manage` (Owner, Admin wszystkie; Trainer tylko własne sesje, backend-enforced)
- `src/features/lesson-logs/schema.ts` — zod: `lessonTopicSchema`, `createHomeworkSchema`, `updateHomeworkSchema`, `homeworkCompletionSchema`
- `src/features/lesson-logs/data.ts` — CRUD: `createLessonTopic` (ON CONFLICT DO UPDATE + `(xmax = 0)` wasInserted), `updateLessonTopic`, `getLessonTopicBySession`, `createHomework`, `updateHomework`, `listHomeworkBySession`, `upsertHomeworkCompletion`, `getCompletionMap`
- `src/features/lesson-logs/manage.ts` — saveLessonTopic (with audit+notify only on insert), createHomeworkEntry (audit+notify), markHomeworkCompletion (audit on every change). ForeignSessionError pattern z `grades/enter.ts`.
- `src/features/lesson-logs/actions.ts` — server actions: `saveLessonTopicAction`, `createHomeworkAction`, `updateHomeworkAction`, `markHomeworkCompletionAction`
- `src/features/lesson-logs/components/lesson-topic-form.tsx` — formularz tematu (title + body textarea)
- `src/features/lesson-logs/components/homework-form.tsx` — formularz zadania (description textarea + dueDate date input)
- `src/features/lesson-logs/components/homework-completion-toggle.tsx` — toggle button per uczestnik (done / not_done)
- `src/app/[locale]/(app)/dashboard/sessions/[sessionId]/page.tsx` — sekcje "Szczegóły lekcji" i "Praca domowa" z listą odznaczeń per uczestnik (gated `lesson_log.manage`)
- `src/features/admin/audit.ts` — akcje `lesson_topic.create`, `homework.create`, `homework_completion.mark` + target types
- `src/lib/adapters/email/` — szablony `lesson-topic-added` i `homework-assigned` + rejestracja w contract/templates/categories
- `src/features/notifications/` — `lesson_topic_added` i `homework_assigned` w types + EMAIL_TEMPLATE_MAP
- `src/features/lesson-logs/schema.test.ts` — 14 unit testów walidacji zod (schema-only, no DB)
- i18n `en.json`/`pl.json` — klucze `lessonLog.*`, `emails.lesson-topic-added`, `emails.homework-assigned`
**DoD:** tsc --noEmit czysty; lint zero nowych błędów; 186/186 testów zielonych (14 nowych). E2E do dopisania w osobnym bloku.

---

