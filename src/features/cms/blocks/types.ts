// Each block component has different props, resolved dynamically by the renderer
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type BlockComponent = React.ComponentType<any>;
